export interface EMapProps {
  /**
   * Width of the map
   *
   * @default 400
   */
  width?: number
  /**
   * Height of the map
   *
   * @default 400
   */
  height?: number
  /**
   * map image
   */
  img: string | HTMLImageElement
}
